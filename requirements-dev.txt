# Local dev + CI. Installs the runtime deps plus testing/lint tooling.
-r layer/requirements.txt

boto3>=1.34
moto[dynamodb]>=5.0
pytest>=8.0
httpx>=0.27          # FastAPI TestClient transport
uvicorn>=0.30        # ASGI server for the local ($0, no-AWS) demo
ruff>=0.5
mypy>=1.10
