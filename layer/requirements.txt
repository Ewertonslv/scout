# Runtime dependencies bundled into the shared Lambda layer.
# boto3/botocore are provided by the Lambda runtime, so they are NOT listed here.
fastapi>=0.110,<1.0
mangum>=0.17,<1.0
pydantic>=2.6,<3.0
